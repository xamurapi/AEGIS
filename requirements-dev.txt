# Development / test dependencies.
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
pytest-bdd>=8.0        # executable Gherkin scenarios (tests/features/*.feature)
coverage>=7.0          # line + branch coverage with a fail_under gate
httpx>=0.27            # required by fastapi.testclient (API endpoint tests)
# Mutation testing needs NO extra dependency: mutmut requires WSL on Windows and
# mutatest 3.1.0 is broken on Python 3.11, so the in-repo harness is used
# instead — `python scripts/mutation_test.py`.
