# Core — required
fastapi>=0.110.0
uvicorn>=0.29.0
websockets>=12.0
pydantic>=2.0
aiofiles>=23.0

# HTTP client for agents and external learning — required
httpx>=0.27.0

# System monitoring
psutil>=5.9.0

# LLM providers — optional (system works without them)
openai>=1.30.0
anthropic>=0.28.0

# Local model & LoRA fine-tuning — optional
torch>=2.2.0
transformers>=4.40.0
peft>=0.10.0
bitsandbytes>=0.43.0
datasets>=2.19.0
accelerate>=0.30.0

# Text-to-speech — optional
pyttsx3>=2.90
