# AEGIS core runtime — everything needed to boot the substrate + dashboard.
# Heavy / optional extras live in separate files:
#   requirements-llm.txt  → hosted LLM providers (DeepSeek/Claude)
#   requirements-ml.txt   → local model + LoRA fine-tuning (multi-GB)

fastapi>=0.110.0
uvicorn>=0.29.0
websockets>=12.0
pydantic>=2.0
aiofiles>=23.0

# HTTP client for agents and external learning
httpx>=0.27.0

# System monitoring
psutil>=5.9.0
