"""AEGIS capability layer — verifiable benchmark, sandbox, skills, evaluator.

This package turns the system's progress signal from self-referential metrics
into an *externally verifiable* one: the pass-rate on a benchmark of tasks whose
answers are checked by deterministic verifiers. Everything else (skill library,
environment, multi-agent solver) hangs off that single ground-truth signal.
"""
