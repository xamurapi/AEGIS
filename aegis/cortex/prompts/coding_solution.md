Implement this function in pure Python.

def {func_name}(...):  # {spec}

It must satisfy these examples and generalize beyond them:
{visible_tests}

Hidden tests decide acceptance, so implement the general rule, not the listed cases.

Rules: pure computation only; imports limited to math/statistics/itertools/functools/re/json/collections/string;
no eval/exec/open/__import__, no I/O, no print.

Respond with ONLY a JSON object:
{
  "code": "def {func_name}(...):\n    ...",
  "explanation": "the algorithm used"
}
