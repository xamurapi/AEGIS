# Аудит кода AEGIS

Полный аудит проведён по трём зонам (ядро/оркестратор, слои, LLM+API+eval+тесты).
Ниже — консолидированный список реальных дефектов, отсортированный по критичности.
Стилистические придирки исключены. По каждому пункту указан файл:строка, риск и
сценарий отказа.

## Раунд 3 — полный аудит (sandbox, control plane, 5 систем, ядро) — ИСПРАВЛЕНО с тестами

Третий проход прицельно покрыл зоны, **исключённые из измерения покрытия** в
`pyproject.toml` (`aegis/api/*`, `aegis/eval/sandbox.py`) — именно там нашлась
критическая уязвимость — плюс пять когнитивных систем и `substrate`.

Все 13 находок исправлены и закрыты регрессионными тестами
(`tests/test_audit_round3.py`, 36 тестов) и Gherkin-сценариями
(`tests/features/safety_and_resilience.feature`, 14 сценариев).
**Каждый баг-тест проверен на «красноту»**: прогон файла тестов на коде до
исправлений (`git worktree` на HEAD) даёт 27 падений, после — 0.

Находки R3-1..R3-10 — статический аудит; R3-11..R3-13 найдены запуском системы
и осмотром интерфейса (см. раздел «Находки визуальной проверки» ниже).

| ID | Sev | Находка | Файл | Фикс |
|---|---|---|---|---|
| **R3-1** | **CRIT** | **Побег из sandbox с RCE.** `check_safe` обходила декораторы, дефолты и тело, но **не аннотации** сигнатуры и не kw-дефолты лямбды. Python вычисляет их при исполнении `def`/`lambda`, поэтому `def solve(p, _z: __import__('os').system('...'))` получал вердикт «безопасно» и затем исполнял произвольный код в дочернем процессе. Подтверждено эмпирически: payload записал файл на диск | `eval/sandbox.py:84-106` | `_visit_signature()` обходит дефолты, kw-дефолты и аннотации **всех** видов параметров + `node.returns` + PEP 695 `type_params`; `visit_Lambda` обходит kw-дефолты |
| **R3-2** | **HIGH** | **Утечка `full_status()` неавторизованным клиентам.** Проверка токена гейтила первую отправку и входящие команды, но сокет добавлялся в `connected_ws` **независимо** от авторизации, а `Substrate` рассылает полный статус в этот список каждый тик — токен-гейт раунда 2 обходился пассивным ожиданием | `api/server.py:768` | В fan-out попадают только авторизованные сокеты |
| **R3-3** | MED | **Параметр со значением 0 невозможно эволюционировать.** `new = old * factor` — ноль неподвижная точка, слот генома круглый год выдавал no-op мутации и жёг benchmark-циклы | `layers/evolution_engine.py:106` | Фолбэк на аддитивный шаг `±MUTATION_MAGNITUDE`, направление по-прежнему чередуется |
| **R3-4** | MED | **Рестарт «принимал» неотсуженную мутацию.** При восстановлении чекпоинта геном чемпиона синхронизировался с живыми параметрами, включая параметр **ожидающего кандидата** → изменение, которое ни один бенчмарк не оценивал, становилось чемпионом; последующий отказ откатывал только `self_mod.parameters`, оставляя чемпиона и живое значение рассинхронизированными | `layers/substrate.py:236-244` | Параметр pending-кандидата исключён из синхронизации |
| **R3-5** | MED | **Одна оборванная строка JSONL уничтожала всю историю опыта.** `_load` парсил лог списковым включением в общем `try` → любая недописанная строка (краш при append) обнуляла все счётчики; `export_examples`/`status` читали ключи напрямую → `KeyError` на строках старой схемы | `layers/feedback_loop.py:46-64,160-179` | Построчный парсинг с пропуском битых строк; чтение через `.get()` с дефолтами |
| **R3-6** | MED | **O(n) чтение файла на каждый append.** `_truncate_if_needed` вызывался после каждой записи и делал `readlines()` всего лога только чтобы посчитать строки | `layers/feedback_loop.py:124-150` | Счётчик `_rows_on_disk`; файл читается, только когда счётчик превысил бюджет |
| **R3-7** | MED | **O(N·E) в графе знаний.** `_degree()` сканировал все списки смежности; он же — ключ сортировки в `_prune()`, `central_nodes()` и `related()`, а `status()` дёргается на каждый тик → на потолке 5000 узлов это квадратичная нагрузка в горячем пути | `layers/cognitive_graph.py:116-132` | Инкрементальный индекс входящих степеней `_in_degree` (не персистится, пересобирается при загрузке — формат на диске не изменился) |
| **R3-8** | MED | **Ответ LLM принимался на веру.** `refine_chain` слайсила `risks`/`plan`/`constraints` без проверки типа: `dict` → `TypeError`, строка → посимвольная итерация; нечисловой `confidence` → `ValueError` | `layers/world_model.py:180-202` | Коэрсинг формы (`_as_list`, `_as_confidence`) |
| **R3-9** | LOW | **Эндпоинт врал об откате.** `/api/state-backup/restore` возвращал `"restored"`, хотя `restore_latest()` только читает снапшот и никак не применяет его к работающей системе | `api/server.py:534-539` | Честный ответ: `status: "loaded"`, `applied: false` + инструкция |
| **R3-10** | LOW | **500 вместо 503 до старта runtime.** Все `/api`-хендлеры разыменовывают глобальный `substrate`, который создаётся только в lifespan | `api/server.py:103-121` | Guard в middleware (503) + guard в WS; маршруты с собственной проверкой токена отвечают 401 **раньше** guard'а, чтобы 503 не выдавал состояние runtime неаутентифицированному клиенту |

### Находки визуальной проверки (запущенная система + браузер)

Эти три дефекта невозможно было найти чтением кода — только запуском системы и
осмотром интерфейса. Все три жили в файлах, **исключённых из измерения покрытия**.

| ID | Sev | Находка | Файл | Фикс |
|---|---|---|---|---|
| **R3-11** | MED | `_semantic_summary` падал с `AttributeError` на не-dict записи семантической памяти: `isinstance`-guard прикрывал только вложенный `relations`, а верхнеуровневые `val.get()` вызывались всегда. Ломал **весь** автономный ответ (режим без LLM) | `api/server.py:346` | Ранний возврат для не-dict + guard на не-dict `relations` |
| **R3-12** | MED | `/api/agents/create` вызывал `agent_system.generate_prompt()` — **метода, которого не существует**. Эндпоинт отвечал HTTP 500 на каждый вызов; ни один тест его не трогал | `api/server.py:519` | Описание агента строится из его собственных полей; заодно проброшен параметр `topic` |
| **R3-13** | MED | Дашборд читал поля, которых нет в `full_status()`: `ev.event_type` (в шине событий поле называется `type`) и `event_bus.published`/`vetoed` (реально `total_events`/`blocked_events`). Журнал событий печатал `undefined` в каждой строке, цветовая маркировка была мертва, счётчики пустые. JS не падает на таких опечатках — оператор видит сломанную панель без единой ошибки | `dashboard/index.html` | Верные имена полей + `tests/test_dashboard_contract.py`: тест сверяет **все 161** обращение дашборда к статусу с реальным payload |

Дополнительно при визуальной проверке сайта удалён мёртвый Cloudflare-скрипт
(`cdn-cgi/.../email-decode.min.js`), дававший 404 в консоли: артефакт сохранения
страницы, обфусцированных email на странице нет.

### Побочное следствие фикса R3-2 (закрыто)

Закрытие утечки WS означало, что при заданном `AEGIS_API_TOKEN` штатный дашборд
переставал получать данные — он подключался к `/ws` без токена и **молча замирал**
(`updateDashboard` выходил по `if (!d.substrate) return`). Плюс все 18 POST-запросов
дашборда уходили без заголовка `X-API-Token` и получали 401. Исправлено в
`dashboard/index.html`: токен берётся из `?token=` и запоминается в `localStorage`,
один shim подставляет заголовок во все `/api`-запросы, а неавторизованное
соединение показывает явный баннер вместо тишины. Проверено вживую в браузере:
без токена — баннер и `Tick: 0`, с токеном — живой поток и `200` на защищённый POST.

### Что осталось незакрытым (осознанно)

- **Sandbox — не настоящая тюрьма.** `check_safe` + `python -I` это defense-in-depth,
  а не изоляция ОС. R3-1 — вторая критическая дыра в этом слое за два раунда
  (первая — R2 `_ = lambda eval: None`), что само по себе довод: статический
  AST-фильтр — хрупкий контур безопасности. Для продакшена нужен контейнер/seccomp
  с запретом сетевого egress. Зафиксировано как архитектурное требование.
- **Эволюционируемые параметры остаются симулированными «ручками»** (см. раунд 2):
  механизм отбора корректен, но `learning_rate`/`attention_heads` причинно не
  влияют на skill-бенчмарк.
- **Тесты не герметичны:** `Substrate()` в тестах подхватывает реальный
  `data/`-каталог разработчика (обнаружено при написании R3-4 — тест поймал чужое
  persisted-состояние). Новые тесты изолируют состояние явно; общий фикс —
  фикстура, подменяющая все `*_DIR` на `tmp_path`, — **не сделан**, это остаток долга.

### R3-14 (качество набора тестов): падения на нагрузке машины — ИСПРАВЛЕНО

Набор падал ~**2 раза из 5**, каждый раз в разном тесте. Общая причина:
`HealthMonitor.check()` читает реальные CPU/память, под нагрузкой полного прогона
health становится `critical`, `MetaRegulation` включает аварийный режим и
выставляет `skip_learning`, после чего тик закономерно не запускает обучение, а
ethics блокирует самомодификацию. То есть тесты проверяли **загрузку машины**.
Плюс два теста держали «работа ещё идёт» через `asyncio.sleep`, который медленный
тик мог пережить. Исправлено фиксацией health во всех восьми хелперах
`_make_substrate`, переводом ожиданий на `asyncio.Event` и изоляцией хранилища
навыков в API-тестах. **17 полных прогонов подряд зелёные.** Подробности — в
`docs/QA.md`, §6.

---

## Раунд 2 — полный аудит всего ядра (6 агентов) — ИСПРАВЛЕНО с тестами

Второй проход покрыл весь `aegis/` + `main.py`. Все находки исправлены (981 тест зелёный):

| Находка | Sev | Фикс | Тест |
|---|---|---|---|
| Побег из sandbox через параметр-экспорт (`_ = lambda eval: None` → RCE) | **CRIT** | Scope-aware `check_safe` (`_SafetyVisitor`): исключение параметра только в его области видимости | `test_audit_round2.py`, `test_sandbox_security.py` |
| Обход blocklist через `from os import system` (голый вызов) | HIGH | Резолвинг from-import алиасов в `code_modifier` и `self_preservation`; блок `from os import *` | `test_audit_round2.py` |
| Обход `open()` write через `io.open`/`os.open` | MED | Детекция write-режима для `io.open`/`os.open` | `test_audit_round2.py` |
| pyttsx3 гонка потоков (`speak`) | HIGH | Один worker-поток + bounded queue (сериализация речи) | `test_motor_cortex_speech_serialization.py` |
| `state_backup.restore_latest` возвращал старый scheduled вместо свежего emergency | HIGH | Сортировка по наносекундному штампу, не по имени | `test_state_backup.py` |
| Evolution: принятые мутации теряются при рестарте | HIGH | Персист `self_mod.parameters` в чекпоинт + ресинк champion | `test_audit_round2.py` |
| `_norm` числовое приведение строк → ложная верификация (число вместо строки) | MED | Type-aware `values_match` (str-спека требует str) | `test_eval_layer.py` |
| memory: KeyError на старом формате + неогр. рост RAM | MED | Защитный `.get()`, мягкие лимиты episodic/semantic, персист `forgotten_total` | `test_memory_hardening.py` |
| self_modification: неогр. рост списков + `train()` без try/except | MED | Кап 200 на все списки + try/except с записью результата | `test_self_modification_hardening.py` |
| FeedbackLoop: счётчики не восстанавливаются | MED | `_load()` реконструирует счётчики из JSONL | `test_audit_round2.py` |
| CognitiveGraph: high-water индекс ломается forgetting'ом | MED | Обработка последнего окна событий (idempotent add) | `test_audit_round2.py` |
| server: блокирующие load-model/build-dataset; sources/analyze/WS без токена | MED | `run_in_executor` + токен на sources/analyze/WS full_status | `test_api_security.py` |
| emotions/self_mod прочие + LOW (uptime, evolve slice, greedy generate, recovery_count, autobiography, empty hidden_tests, env tasks=[]) | LOW | Исправлено | соответствующие тесты |

**Известное ограничение (не однострочный фикс):** мутируемые Evolution-параметры
(`learning_rate`, `attention_heads`, …) — симулированные «ручки», причинно не
влияющие на skill-бенчмарк, поэтому судейство эволюции опирается на дрейф бенчмарка
от других изменений. Механизм (мутация→бенчмарк→откат/приём, тайминг M3 исправлен)
корректен, но для валидной селекции параметр должен реально влиять на измеримую
метрику — это архитектурная доработка, а не баг.

---


> Пять новых систем (`world_model`, `cognitive_graph`, `evolution_engine`,
> `goal_intelligence`, `feedback_loop`) написаны уже с учётом этих находок:
> атомарная запись состояния, ограниченный рост структур, отсутствие блокирующего
> I/O в async-путях, все хуки в tick обёрнуты в `try/except`. См. `docs/QA.md`.

## Статус исправлений

Исправлено с тестами (все **915 тестов зелёные**):

| Находка | Sev | Что сделано | Тесты |
|---|---|---|---|
| **C1** sandbox-побег через `__builtins__` | CRIT | Блок всех dunder-**имён** в `sandbox.check_safe` | `test_sandbox_security.py` |
| **C2** prompt-injection → самомодификация кода | CRIT | Автономная самомодификация **кода** сделана opt-in (`AEGIS_CODE_SELF_MOD_ENABLED`, по умолчанию OFF) + defense-in-depth в методе | `test_code_selfmod_optin.py` |
| **A1** `open(...,'w')` не блокировался | SEC | AST-детекция write/append (и нелитерального) режима в `code_modifier` | `test_code_modifier_security.py` |
| **H1** data race на общей `SkillLibrary` | HIGH | `threading.RLock` вокруг всех операций; читатели возвращают снапшоты | `test_skill_library_concurrency.py` |
| **H2** блокирующая загрузка модели в корутине | HIGH | `load_model()` вынесена в `run_in_executor` | `test_weight_modifier_h2.py` |
| **H3** нет таймаутов на LLM | HIGH | `LLM_TIMEOUT_SECONDS` на SDK-клиентах + `wait_for` на локальном вызове | `test_llm_timeout.py` |
| **H4** CSWSH на WebSocket (+H7 утечка) | HIGH | Проверка `Origin` до accept + очистка в `finally` | `test_api_security.py` |
| **M4** несуществующий дефолт Claude | MED | `claude-sonnet-5`, переопределяемый через env | — |
| **M5** типы LLM-JSON не валидируются | MED | Коэрсинг `_coerce_int/_coerce_float` + guard'ы типов в 4 местах substrate | `test_substrate_m5.py` |
| **M6** фоновые задачи не гасятся на shutdown | MED | `Substrate.cancel_background_tasks()` + вызов в lifespan | `test_substrate_shutdown.py` |
| **M8** опасная связка host+token | SEC | `config.network_exposure_warning` + warning при импорте | `test_api_security.py` |
| **A3** неатомарная запись стат-файлов | LOW | Общий `aegis/_atomic.atomic_write_text` в 5 местах | `test_atomic_write.py` |
| **L1** bump версии падал на невалидной строке | LOW | Переиспользован толерантный `self_mod._bump_patch` | `test_audit_low_fixes.py` |
| **L2** исключение из veto не перехватывалось | LOW | `try/except` в `event_bus.publish`, fail-closed (block) | `test_audit_low_fixes.py` |
| **L4** необработанный `int/float` env на импорте | LOW | Хелперы `_env_int/_env_float` с fallback+warning (24 парса) | `test_audit_low_fixes.py` |
| **L3** история хранила живые ссылки на payload | LOW | Снапшот payload (deepcopy) при записи + копии из `get_history/get_blocked` | `test_audit_low_fixes.py` |
| **L5** подстрочное сопоставление имени файла в ethics | LOW | Сопоставление по basename, не подстроке | `test_audit_low_fixes.py` |
| **L6** blocklist обходится рефлексией | LOW | Блок exploit-only dunder'ов (`__subclasses__`/`__globals__`/…) в `code_modifier` и `self_preservation` (легитимные `__init__`/`__file__`/`__name__` проходят) | `test_audit_low_fixes.py` |
| **L8** `asyncio.get_event_loop()` устаревший | LOW | Заменён на `get_running_loop()` (14 мест) | регрессия async-путей |
| **L9** GET чтения исходников без токена | LOW | Проверка токена в эндпоинте | `test_audit_low_fixes.py` |
| **L10** сравнение токена не постоянного времени | LOW | `hmac.compare_digest` в middleware + WS + GET | `test_audit_low_fixes.py` |

**Все находки аудита закрыты.** 923 теста зелёные.

## CRITICAL

**C1. Побег из песочницы skills через `__builtins__`** — `aegis/eval/sandbox.py:96-107`
AST-проверка `check_safe` применяет правило dunder-имён только к `ast.Attribute.attr`,
но не к `ast.Name.id`. Имя `__builtins__` не в `FORBIDDEN_CALLS`, а `.eval`/`.exec`
не являются dunder → `__builtins__.eval("__import__('os').system(...)")` проходит
фильтр и исполняется. Так как skills синтезируются автономно, это полноценный RCE
без участия человека. **Фикс:** блокировать любые `__*__` имена в ветке `ast.Name`,
либо запускать в реальном OS-sandbox/контейнере.

**C2. Цепочка indirect prompt injection → перезапись собственного кода** —
`substrate.py` (external_learning → semantic-память → LLM-промпт → code self-mod)
Непроверенный внешний контент (Wikipedia/arXiv/фиды) попадает в semantic-память,
оттуда в промпты, а тот же LLM-канал генерирует полные перезаписи файлов пакета
`aegis`, применяемые на диск и исполняемые после рестарта вне sandbox. Защита —
только чёрные списки паттернов и эвристики этики. **Фикс:** белый список изменяемых
зон, обязательное ревью человека или изолированный прогон применённого кода.

## HIGH

**H1. Data race на общей `SkillLibrary`/`Evaluator` между executor-потоками** —
`substrate.py:684-707, 725-880` + `eval/skill_library.py`
`environment.step` (executor, каждые 2 тика) итерирует `skill_library`, пока
`_skill_synthesis`/`_optimization` (event loop) делают `add()/remove()/save()`.
На тиках 200/400… `_eval_task` и `_skill_synth_task` стартуют одновременно.
Отказы: `RuntimeError: dictionary changed size during iteration`; порча `skills.json`;
искажённый замер `before/after`, из-за которого гейт принимает вредный skill или
ретайрит рабочий. **Фикс:** единый `asyncio.Lock`/`threading.Lock` вокруг доступа к
библиотеке.

**H2. Блокирующая загрузка модели в корутине** — `weight_modifier.py:224-231`
`load_model()` вызывается прямо в `async def train`, а не через `run_in_executor`.
Загрузка/квантизация LLM (гигабайты) замораживает весь tick-цикл на минуты.
**Фикс:** вынести `load_model` в executor.

**H3. Нет таймаутов на сетевые вызовы LLM** — `llm.py:246-251, 278-284`
`chat.completions.create` / `messages.create` без `timeout=` и `asyncio.wait_for`.
Зависший провайдер вешает `/api/chat` и фоновые задачи на дефолт SDK (до 10 мин).
**Фикс:** явный `timeout` + `asyncio.wait_for`.

**H4. CSWSH / нет проверки Origin на WebSocket** — `api/server.py:705-727`
WS не подчиняется CORS и не проверяет `Origin`. При дефолте (пустой токен, loopback)
любая веб-страница подключается к `ws://127.0.0.1:8888/ws`, читает `full_status()` и
активирует kill-switch. **Фикс:** проверка `Origin`, требование токена на WS.

## MEDIUM

- **M1. Блокирующая генерация в event loop** — `weight_modifier.py:454-473` (`generate()` синхронный).
- **M2. Блокирующий файловый I/O в async** — `dataset_builder.py:167-173` (пишет `.jsonl` без executor).
- **M3. Нет общего таймаута на прогон агентов за тик** — `agent_system.py:155-208` (5+ агентов × 12s).
- **M4. Неверный дефолт модели Claude** — `config.py:43` `CLAUDE_MODEL="claude-opus-4-6"` не существует → все Claude-вызовы падают 404.
- **M5. Типы LLM-JSON не валидируются** — `substrate.py:500-503, 427-435, 1138-1148` (`"chosen":"2"` → TypeError срывает остаток тика).
- **M6. `stop()` может отказать + фоновые задачи не гасятся на shutdown** — `substrate.py:1290-1301`, `api/server.py:53-61`.
- **M7. Блокирующий/неатомарный checkpoint в event loop** — `substrate.py:1187, 1202-1205` (сериализация всей памяти в `_reflect`). *(В новых системах и в `_save_checkpoint` уже сделана атомарная запись.)*
- **M8. Опасная связка конфигурации не детектируется** — `config.py:23-30` (`AEGIS_API_HOST=0.0.0.0` + пустой токен = неаутентифицированный сетевой доступ к самомодификации).
- **M9. «repair attempt» в `_skill_synthesis` не реализован** — `substrate.py:771-776` (повтор с тем же промптом).
- **M10. Неограниченный рост памяти в RAM** — `memory.py:63-96` (усечение только в `save()`).
- **M11. pyttsx3 не потокобезопасен** — `motor_cortex.py:40-51` (перекрывающиеся `speak` → падение).
- **M12. Тесты `Substrate()`/`LLMEngine()` не изолированы от `data/`** — `test_eval_layer.py`, `test_llm_utils.py`.
- **M13. Утечка/гонка WS-соединений** — `api/server.py:712-732` (нет `finally`), `28-38` (итерация при модификации).

## LOW

- **L1.** Bump версии падает на невалидной строке версии из checkpoint — `substrate.py:1081-1083`.
- **L2.** Исключение из `_veto_fn` не перехватывается — `event_bus.py:69-70`.
- **L3.** История событий хранит живые ссылки на `payload` — `event_bus.py:59-67`.
- **L4.** Необработанные `int()/float()`/`mkdir` на импорте `config.py` валят весь пакет.
- **L5.** Подстрочное сопоставление имени файла в этике — `ethics_core.py:218-222` (`myconfig.py` ложно совпадёт).
- **L6.** Обход blocklist `CodeModifier`/`SelfPreservation` через рефлексию/алиасинг — `code_modifier.py:14-40`, `self_preservation.py:28-81`.
- **L7.** Неатомарная запись stat-json — `llm.py:160-163`, `weight_modifier.py:54-66`, `evaluator.py:57-62`.
- **L8.** `asyncio.get_event_loop()` в корутинах (устаревший API) — `substrate.py` (множество мест).
- **L9.** GET чтения исходников не требует токена — `api/server.py:615-621`.
- **L10.** Сравнение токена не постоянного времени — `api/server.py:71` (нужен `hmac.compare_digest`).

## Дополнительно найдено при написании тестов (2-я волна)

Эти дефекты всплыли, когда покрытие остальных модулей доводилось до ≥85%:

- **A1 (security). `open(..., 'w')` не блокируется валидатором** — `code_modifier.py`.
  `validate_safety` ловит только две hardcoded-подстроки (`open('/etc`, `open('C:\Windows`),
  а `open` отсутствует в `DANGEROUS_CALL_NAMES`. Код `open('any.txt','w').write(...)`
  проходит и AST-, и подстрочную проверку → запись в произвольный файл мимо
  safety-слоя. Усугубляет C1/C2.
- **A2. Многословный `backup_type` обрезается** — `state_backup.py` (`_rotate_backups`,
  `list_backups`). Тип берётся как `filename.split("_")[1]`, поэтому `"pre_shutdown"`
  усекается до `"pre"` — разные типы ротируются вместе и мислейблятся. Латентно
  (сейчас только `scheduled`/`emergency`), но реальный баг при compound-типах.
- **A3. Неатомарная запись стат-файлов (подтверждено)** — `llm.py:161`,
  `weight_modifier.py:64` пишут `write_text` напрямую; краш в момент записи → битый
  JSON → загрузка молча сбрасывает всю накопленную статистику в ноль. (См. L7.)
- **A4. Бюджет LLM тратится на упавшие вызовы** — `llm.py:314-315` инкрементит
  счётчик и таймер ДО вызова провайдера; неудачный вызов всё равно съедает бюджет.
- **A5. `rollback_to_checkpoint` пишет состояние до проверки загрузки** —
  `weight_modifier.py:429-431`: `_save_stats()` до `load_model()`; при провале
  загрузки на диске остаётся указатель на неработающий чекпоинт.

Мёртвый/защитный код (не баги, задокументировано): недостижимая ветка
`recovery→normal` в `meta_regulation.py:46-47`; дефолтные fallback'и в `dreams.py:69`,
`emotion_nlp.py:48`; дважды присваиваемый `ethics_approved` в `self_modification.py:262-263`.

## Сделано хорошо (для ориентира)

Sandbox для skills (кроме C1), атомарная запись `memory.save()`, защита
`tick()`/`run()` от падений, сброс `llm_thinking` в `finally`, детач тяжёлых задач
(бенчмарк/LoRA) через `create_task`+`run_in_executor` с хэндлами против перекрытия.

## Как подключается новый слой к Substrate (для интеграции)

1. Атрибут в `Substrate.__init__`.
2. Вызовы в нужной фазе `tick()` с периодом `tick_count % N` (константы в `config.py`).
3. Строка `"<name>": module.status()` в `full_status()`.
4. Persistence: `save()` в `_save_checkpoint()`; тяжёлые операции — по образцу
   `_run_benchmark` (detached task + executor), с учётом H1 (общие структуры между
   потоками сейчас без локов).
