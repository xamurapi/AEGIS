# QA — процедуры, метрики качества и контроль покрытия

Документ описывает процесс контроля качества пяти ключевых когнитивных систем
AEGIS (`World Model`, `Cognitive Graph`, `Evolution Engine`, `Goal Intelligence`,
`Real-world Feedback Loop`) и то, как воспроизвести все проверки.

## 1. Уровни тестирования

| Уровень | Что проверяет | Файлы |
|---|---|---|
| Юнит-тесты | Логика каждой системы в изоляции (persistence в `tmp_path`) | `tests/test_world_model.py`, `test_cognitive_graph.py`, `test_evolution_engine.py`, `test_goal_intelligence.py`, `test_feedback_loop.py` |
| Интеграционные | 5 систем внутри реального tick-цикла `Substrate` (offline, без сети/LLM) | `tests/test_higher_systems_integration.py` |
| BDD (Gherkin) — поведение систем | Поведение пяти систем на языке спецификации | `tests/features/higher_systems.feature` + `tests/test_bdd_higher_systems.py` |
| BDD (Gherkin) — безопасность и устойчивость | Гарантии sandbox, control plane, эволюции, лога опыта, обработки ответов LLM | `tests/features/safety_and_resilience.feature` + `tests/test_bdd_safety_resilience.py` |
| Регрессия аудита | Каждая находка аудита закрыта тестом, который был красным до фикса | `tests/test_audit_low_fixes.py`, `test_audit_round2.py`, `test_audit_round3.py` |
| Ёмкость и удержание | Поиск рисков не перечисляет всю таблицу связей; лимиты структур следуют за **измеренной** задержкой тика; вытеснение сохраняет информативное, а не частое | `tests/test_capacity_and_retention.py` |
| Замыкание поведения | Выученная структура **меняет поведение**, а не только накапливается: решение идёт от выученной полезности, известный риск снижает уверенность, граф выбирает тему обучения, журнал опыта доходит до датасета | `tests/test_behavior_closure.py` |
| Регрессия ядра | Существующие тесты ядра не сломаны интеграцией | `tests/test_*.py` (память, этика, self-mod, eval, …) |

### Правило для регрессий аудита

Тест на находку аудита обязан быть **доказуемо красным до исправления**.
Проверка выполняется прогоном нового файла тестов на коде до фикса:

```bash
git worktree add /tmp/aegis_head HEAD
cp tests/test_audit_round3.py /tmp/aegis_head/tests/
cd /tmp/aegis_head && python -m pytest tests/test_audit_round3.py -q   # ожидаем падения
git worktree remove /tmp/aegis_head --force
```

Для раунда 3 это дало **27 падений из 36** (остальные 9 — охранные тесты вида
«легитимный сценарий не должен сломаться», они зелёные в обеих версиях).

## 2. Команды запуска

```bash
# Все зависимости для QA
pip install -r requirements-dev.txt

# Весь набор тестов
python -m pytest -q

# Только пять систем
python -m pytest tests/test_world_model.py tests/test_cognitive_graph.py \
    tests/test_evolution_engine.py tests/test_goal_intelligence.py \
    tests/test_feedback_loop.py tests/test_higher_systems_integration.py \
    tests/test_bdd_higher_systems.py -q

# Только Gherkin-сценарии (обе feature-спецификации)
python -m pytest tests/test_bdd_higher_systems.py tests/test_bdd_safety_resilience.py -q

# Только регрессии аудита
python -m pytest tests/test_audit_low_fixes.py tests/test_audit_round2.py \
    tests/test_audit_round3.py -q

# Покрытие с гейтом (fail_under=85 в pyproject.toml)
python -m coverage run -m pytest -q
python -m coverage report            # текстовый отчёт
python -m coverage html -d data/coverage_html   # HTML-отчёт

# Мутационное тестирование
python scripts/mutation_test.py                  # все 5 систем
python scripts/mutation_test.py evolution        # одна система по ключевому слову
```

## 3. Метрики качества (прогон от 26.07.2026)

### 3.1. Сводка

| Метрика | Было (раунд 2) | Стало | Гейт |
|---|---:|---:|---:|
| Тестов, все зелёные | 981 | **1380** (+2 skip) | — |
| Покрытие (branch, весь пакет) | 93%¹ | **92%**¹ | **90%** |
| Мутационный балл | 81.0% (362/447) | **99.8%** (446/447) | 100%² |
| Модулей со 100% мутационным баллом | 6 из 13 | **12 из 13** | — |

¹ Числа не сравнимы напрямую: раньше гейт **не измерял** `aegis/api/*` и
`aegis/eval/sandbox.py`. Теперь измеряется весь пакет (+487 и +141 инструкций),
поэтому 92% «шире» прежних 93%. См. §4.

² Harness возвращает ненулевой код выхода при любом выжившем мутанте; единственное
задокументированное исключение — эквивалентный мутант ниже.

### 3.2. Пять ключевых систем

| Система | Покрытие (branch) | Мутационный балл |
|---|---:|---:|
| `world_model.py` | 99% | **100%** (31/31) |
| `cognitive_graph.py` | 93% | **96.8%** (30/31)³ |
| `evolution_engine.py` | 95% | **100%** (14/14) |
| `goal_intelligence.py` | 97% | **100%** (23/23) |
| `feedback_loop.py` | 88% | **100%** (20/20) |

### 3.3. Мутационное тестирование — все цели

`python scripts/mutation_test.py [<ключевое слово>]`

| Модуль | Было | Стало | Что закрыло разрыв |
|---|---:|---:|---|
| `eval/sandbox.py` | **25.9%** | **100%** (27/27) | `test_sandbox_mutation.py` — точность гейта + все пути отказа обоих раннеров |
| `layers/emotions.py` | 50.0% | **100%** (66/66) | `test_behaviour_mutation.py` — точные значения VAD-арифметики |
| `layers/health_monitor.py` | 57.7% | **100%** (26/26) | то же + тест fallback-пути без `psutil` |
| `layers/meta_regulation.py` | 62.9% | **100%** (35/35) | то же — исчерпывающее закрепление автомата режимов |
| `event_bus.py` | 83.3% | **100%** (6/6) | fail-closed veto (`test_mutation_gaps.py`) |
| `layers/feedback_loop.py` | 85.0% | **100%** (20/20) | пороги усечения лога наблюдаются независимо |
| `layers/self_preservation.py` | 95.7% | **100%** (46/46) | детект летальных вызовов через from-import с алиасом |
| `layers/cognitive_graph.py` | 93.5% | **96.8%** (30/31)³ | учёт входящих степеней при отсечении |
| `layers/world_model.py` | 100% | **100%** (31/31) | — |
| `layers/evolution_engine.py` | 100% | **100%** (14/14) | — |
| `layers/goal_intelligence.py` | 100% | **100%** (23/23) | — |
| `layers/ethics_core.py` | 100% | **100%** (41/41) | — |
| `layers/goal_engine.py` | 100% | **100%** (81/81) | — |

**Самый важный результат: `sandbox.py` — 25.9% → 100%.** Это модуль с наибольшей
ценой ошибки (единственный барьер между самописным кодом навыков и хостом), и он
же имел худший мутационный балл. Разрыв был не случаен: в него не смотрело ни
покрытие (исключён из гейта), ни ассерты (вся половина модуля с обработкой ошибок
не вызывалась ни одним тестом). Там же аудит нашёл CRITICAL RCE (R3-1).

**Что мутационное тестирование показало сверх покрытия:** ветку `except` у veto в
`event_bus` тесты *исполняли* (покрытие 98%), но не проверяли её результат —
перевернув `allowed = False` на `True`, весь набор оставался зелёным. То есть
«упавшая проверка безопасности пропускает событие» не ловилось. Покрытие меряет
исполнение, мутационный балл — наличие утверждений.

³ Единственный выживший мутант во всём проекте — `cognitive_graph.find_path`
(`or → and` в проверке существования узлов). **Доказуемо эквивалентный:** если
хотя бы один конец отсутствует, обе версии возвращают `None` — при `and` BFS
стартует, но `adj` не содержит отсутствующего узла, очередь пустеет, результат
тот же. Не дефект.

**Улучшения harness:** (1) корректная индексация вложенных BinOp одного типа на
одной строке (`x/2*100`); (2) отбрасывание вырожденных мутантов; (3) побайтовое
восстановление исходников; (4) в цели добавлен `eval/sandbox.py`, а к каждой цели
подключены соответствующие аудит-тесты.

## 4. Контроль покрытия (quality gate)

Настроен в `pyproject.toml`:

```toml
[tool.coverage.run]
branch = true
source = ["aegis"]
omit = ["*/__pycache__/*"]   # больше НИЧЕГО не исключается

[tool.coverage.report]
fail_under = 90          # сборка падает, если покрытие ниже
show_missing = true
exclude_lines = ["logger.exception", "logger.warning", ...]  # обработчики ошибок
```

### Что изменилось и почему

Раньше из измерения были исключены `aegis/api/*` («покрывается интеграционно») и
`aegis/eval/sandbox.py`. Аудит раунда 3 нашёл в этих двух местах **критический
побег из sandbox с RCE** и **два мёртвых эндпоинта** (один отвечал 500 на каждый
вызов). Совпадение не случайное: неизмеряемый модуль — незащищённый модуль.
Исключения сняты, гейт поднят 85 → 90, текущее покрытие всего пакета — 92%.

Что для этого понадобилось:

| Модуль | Было | Стало | Чем |
|---|---:|---:|---|
| `eval/sandbox.py` | не измерялся | **91%** | `test_sandbox_mutation.py` (24 теста) |
| `api/server.py` | не измерялся | **75%** | `test_api_endpoints.py` (65 тестов) |

`test_api_endpoints.py` перечисляет GET-маршруты **из таблицы роутов самого
приложения**, поэтому новый эндпоинт попадает под smoke-проверку автоматически.

### Остаток непокрытого

`substrate.py` — 74%: большой async-оркестратор, непокрытое — ветки, требующие
живого LLM и многочасовых прогонов (обучение LoRA, синтез навыков). Остальные
модули ≥ 88%. Непокрытыми также остаются ветки `except → logger.warning`,
исключённые из подсчёта осознанно.

## 5. Мутационное тестирование

Инструмент: собственный харнесс `scripts/mutation_test.py` (mutmut не работает на
Windows без WSL; mutatest 3.1.0 несовместим с Python 3.11).

**Как работает:** для каждого модуля перечисляются мутанты через AST — переворот
сравнений (`<` ↔ `≥`), арифметики (`+` ↔ `−`, `*` ↔ `/`), булевых операторов
(`and` ↔ `or`), булевых констант (`True` ↔ `False`). Каждый мутант записывается в
файл, прогоняются его тесты, файл всегда восстанавливается (`finally` + резервная
копия `.mut.bak` для устойчивости к жёсткому убийству процесса). Мутант **убит**,
если тесты упали, и **выжил**, если прошли — выживший указывает на пробел в
ассертах.

**Эквивалентные мутанты** исключаются по построению: булевы флаги
`exc_info`/`ensure_ascii`/`indent`/`sort_keys` в вызовах логирования и сериализации
не меняют логику.

**Гейт:** `scripts/mutation_test.py` возвращает ненулевой код выхода, если выжил
хотя бы один (не-эквивалентный) мутант — пригодно для CI.

## 6. Изоляция тестов

Все юнит-тесты пяти систем передают `store_path=tmp_path/...`, поэтому не читают и
не пишут в рабочий каталог `data/`. Интеграционные тесты нейтрализуют сеть
(`agent_system.run_due_agents`), LLM (`llm.enabled=False`) и sandbox
(`environment.step` заглушён), обеспечивая детерминированный offline-прогон.

**Известное ограничение (реальный инцидент).** `Substrate()` читает рабочий
каталог `data/` разработчика. При написании теста R3-4 это дало ложное падение:
тест подхватил pending-кандидата эволюции из реального прогона. Правило:
**любой тест, создающий `Substrate`, обязан подменить состояние**, которое
проверяет:

```python
sub = Substrate()
sub.evolution = EvolutionEngine(store_path=tmp_path / "lineage.json")
monkeypatch.setattr(sub, "_checkpoint_path", tmp_path / "latest.json")
```

Общий фикс (фикстура, подменяющая все `*_DIR` на `tmp_path`) не сделан —
зафиксирован как долг в `docs/АУДИТ.md`.

### Нестабильные тесты: найдено и устранено

При многократном прогоне набора обнаружилось, что он падал примерно **в 2 случаях
из 5** — и падал в разных тестах. Причина оказалась общей и неочевидной:

> `HealthMonitor.check()` читает **реальные** CPU и память через `psutil`. Во время
> полного прогона (сотни подпроцессов sandbox) загрузка CPU превышает порог, health
> становится `critical`, `MetaRegulation` переходит в аварийный режим и выставляет
> `skip_learning`, а `ethics` блокирует самомодификацию. Тесты падали **на нагрузке
> машины, а не на дефекте продукта**.

Проверено воспроизведением: при подстановке `health.check() -> "critical"`
директива `skip_learning` становится `True`, и тик закономерно не запускает
обучение.

Устранено:

1. Все восемь хелперов `_make_substrate` фиксируют health как `healthy` — ровно
   тем же приёмом, которым они уже нейтрализуют сеть, LLM и sandbox.
2. Ожидания «задача ещё выполняется» переведены с `asyncio.sleep(...)` на
   `asyncio.Event`, который освобождает **сам тест**: медленный тик больше не
   может «пережить» фиктивную работу. Блокирующий тик теперь даёт чистый
   `wait_for`-таймаут вместо вердикта, зависящего от таймингов.
3. Модульная фикстура `test_api_endpoints.py` уводит хранилище навыков в
   `tmp_path`, чтобы `/api/eval` не сериализовал библиотеку, которую в этот
   момент перезаписывает другой тест.

**Результат: 17 полных прогонов подряд без единого падения.**

Правило: тест, который зависит от загрузки машины, времени или общего каталога
`data/`, считается дефектом теста. Ожидание «работа ещё идёт» выражается
событием, а не сном.

## 7. Визуальная проверка (обязательна для UI и control plane)

Три дефекта раунда 3 (R3-11..R3-13) **невозможно** найти чтением кода или
юнит-тестами: JS не падает на опечатке в имени поля, он рисует `undefined`.
Поэтому в чек-лист входит запуск и осмотр:

```bash
# 1. Запустить рантайм на отдельном порту
AEGIS_API_TOKEN=demo python -m uvicorn aegis.api.server:app --port 8900

# 2. Открыть http://127.0.0.1:8900/ — ожидается баннер "Not authorized", Tick: 0
# 3. Открыть http://127.0.0.1:8900/?token=demo — ожидается живой поток данных
# 4. Пройти все вкладки; на странице не должно быть ни "undefined", ни "NaN"
# 5. Консоль браузера — без ошибок (кроме favicon 404)
```

Регрессия этого класса закрыта автоматически: `tests/test_dashboard_contract.py`
сверяет **все 161** обращение дашборда к `full_status()` (включая обращения через
промежуточные переменные) с реальным payload.

## 8. Чек-лист перед мержем

1. `python -m pytest -q` — все тесты зелёные (сейчас 1380).
2. `python -m coverage run -m pytest -q && python -m coverage report` — покрытие ≥ 90%.
3. `python scripts/mutation_test.py` — мутационный балл 100% (кроме задокументированного эквивалентного мутанта).
4. Новые публичные методы систем имеют: юнит-тест, ассерт на границы, и (для нового поведения) Gherkin-сценарий.
5. Любой новый слой подключён к `Substrate` по чек-листу из `docs/АУДИТ.md` (атрибут → фаза tick → `full_status()` → persistence).
6. Тест на находку аудита **проверен на «красноту»** на коде до фикса (см. §1).
7. Изменения в дашборде/API — пройдена визуальная проверка по §7.

---

# Приложение. Цикл этапов 4–5 (прогон от 28.07.2026)

Раздел дописан после аудита, доводки этапа 4 и сдачи этапа 5. Всё, что ниже, —
измеренное, а не заявленное; каждая цифра сопровождается командой.

## A1. Гейты сборки

Ни один коммит не сдан, пока все шесть не зелёные.

| # | Гейт | Команда | Порог | Факт |
|---|---|---|---|---|
| 1 | Тесты | `python -m pytest -q` | 100% | **3125 passed, 2 skipped** |
| 2 | Покрытие ветвей пакета | `python -m pytest --cov=aegis` | ≥90% | **94%** |
| 3 | Покрытие новых модулей | то же, по модулю | ≥95% | см. A3 |
| 4 | Мутационный балл | `python scripts/mutation_test.py` | 0 выживших | см. A4 |
| 5 | Отсутствие заглушек | `python scripts/check_no_stubs.py` | 0 находок | **OK, 105 файлов** |
| 6 | Неопределённые имена | `python scripts/check_undefined_names.py aegis scripts tests` | 0 находок | **OK** |

Плюс приёмочные харнессы (A5) и неприкосновенность предохранителей
(`tests/test_guarded_files.py`, Приложение H ТЗ).

### Откуда взялся гейт №6

Он появился после дефекта, дожившего до аудита. `phases/act.py` собирал
`Event(...)` и читал `Layer.GOAL_ENGINE`, не импортировав ни того ни другого.
Строка лежала на ветке **успеха** — успешный вызов любопытства, — поэтому все
тесты шли по ветке отказа: набор был зелёным, модуль показывал 88% покрытия, а
фаза ACT обрывалась на середине каждый раз, когда вызов удавался, унося с собой
шаг среды, бенчмарк, эволюцию и синтез навыков.

Ни покрытие, ни мутационное тестирование такого не ловят: неверного утверждения
нет, мутировать нечего — имени просто не существует. Гейт задаёт другой вопрос,
и задаёт его каждому модулю: **связано ли где-нибудь каждое имя, которое этот
файл читает?**

## A2. Дефекты, найденные и закрытые в этом цикле

| № | Дефект | Последствие | Закрыт |
|---|---|---|---|
| A1 | `Event`/`Layer` без импорта в `phases/act.py` | `NameError` рвал фазу ACT при каждом удачном curiosity-вызове | `test_audit_stage4_fixes.py` + гейт №6 |
| A2 | `acquire()` списывал `min_interval` за намерение | одно вето этики глушило `train_weights` на 1000 тиков | `test_audit_stage4_fixes.py` |
| A3 | Новые модули этапа 4 ниже требуемых 95% покрытия | непроверенные деградационные пути | `test_stage4_edges.py` (69 тестов) |
| A4 | Мутационный харнесс не знал модулей этапа 4 | гейт не покрывал новый код | `scripts/mutation_test.py` |
| A5 | Нет статического гейта на неопределённые имена | см. выше | `test_no_undefined_names.py` |
| A6 | Мутационный балл планировщика 60.9%, DECIDE 49.0% | арифметика скоринга ничем не закреплена | `test_planner_mutation.py`, `test_decide_mutation.py` |
| A7 | Аренда запланированного действия, которым владеет расписание ACT, не возвращалась | `finalize_tick` списывал полную смету: 12 000 фантомных токенов за 2000 тиков, 3816 отказов, треть измеренного преимущества планировщика | `test_audit_stage4_fixes.py` |
| A8 | `ab_planner.py` перенаправлял `cfg.*`, а модули связывают константу при импорте | «изолированный» харнесс писал в живой `data/` репозитория; два прогона не были независимы | `test_harness_determinism.py` |
| A9 | Held-out бенчмарк выполняется отсоединённой задачей, и тик приземления её результата зависит от планирования подпроцессов | **оба приёмочных харнесса невоспроизводимы**: два прогона одного кода расходятся (замерено — тик 56 из 60) и дают разные цифры. Так планировщик был записан сначала как +113%, потом как +84% | `test_harness_determinism.py` |
| A10 | Активное правило действует всегда, значит контрольной группы нет | любая переоценка отзывала бы правило за отсутствием данных — требование §M3.5 «при потере значимости → retired» было неисполнимо | `ACTIVE_HOLDOUT_BLOCKS` + `test_policy_mutation.py` |

Отдельно: `ruff --fix` в начале аудита затронул `ethics_core.py` и
`self_preservation.py`, которые Приложение H запрещает менять без отдельного
аудита безопасности. Правки откачены, и теперь пять файлов-предохранителей
защищены хеш-тестом `test_guarded_files.py`.

## A3. Покрытие модулей этапов 4–5

Требование ТЗ §3.7 для новых модулей — ≥95%.

| Модуль | Покрытие |
|---|---|
| `layers/planner.py` | 100% |
| `layers/executors.py` | 100% |
| `layers/phases/decide.py` | 99% |
| `layers/policy/store.py` | 100% |
| `layers/policy/rules.py` | 99% |
| `layers/policy/counterfactual.py` | 95% |
| `layers/policy/__init__.py` | 99% |

**Важно про измерение.** Покрытие нельзя считать во время правки исходников:
номера строк смещаются, и отчёт становится бессмысленным. В этом цикле такой
прогон однажды показал 86% там, где было 94%. Прогон с `--cov` — отдельный шаг,
во время которого `aegis/` не редактируется.

## A4. Мутационное тестирование этапов 4–5

| Модуль | Мутантов | Первый прогон | После доработки |
|---|---|---|---|
| `layers/planner.py` | 46 | 60.9% | **100%** |
| `layers/phases/decide.py` | 51 | 49.0% | **100%** |
| `layers/executors.py` | 0 | — | — (чистая делегация) |
| `layers/policy/store.py` | 16 | 81.2% | **100%** |
| `layers/policy/rules.py` | 48 | 93.8% | **100%** |
| `layers/policy/counterfactual.py` | 25 | 80.8% | **100%** |
| `layers/policy/__init__.py` | 28 | 85.7% | **100%** |

Все 18 выживших мутантов планировщика были одного рода: набор проверял, что
число получилось и что планы ранжируются, но никогда — что число **именно
такое**. Мутант, превративший `sum(rate)/10` в `sum(rate)*10`, по-прежнему
возвращал уверенность и проходил все тесты, при этом одного запомненного провала
хватало, чтобы обрушить уверенность до пола. `test_planner_mutation.py` и
`test_decide_mutation.py` закрепляют арифметику, а не поведение.

Пятнадцать выживших мутантов политики распались на три группы, и каждая — свой
способ для набора тестов быть убедительным и пустым: **пути хранилищ по
умолчанию** (везде передавался явный путь, поэтому ветка `store_path or …`
никогда не исполнялась), **арифметика, которую никто не закрепил** (проверялось,
что число получилось и что знак верный — множитель в десять раз проходил), и
**граничные условия окна переоценки и рук испытания** — то есть ровно те места,
где неверный ответ бесшумен по построению.

Два мутанта оказались **эквивалентными из-за дублирования порогов**: внешняя
проверка в `flush` и внутренняя в `_truncate` сравнивались с одним и тем же
`MAX_SHADOW_ROWS * 2`, поэтому изменение любой из них ничего не меняло. Вместо
пометки «эквивалентен» пороги разведены: внешний остался дешёвой проверкой по
счётчику, внутренний сравнивает с бюджетом напрямую, а число фактических
перезаписей выведено в `status()` — теперь каждый порог наблюдаем отдельно.

## A5. Приёмочные харнессы

### Этап 4 — планировщик (§M2.8)

```
python scripts/ab_planner.py               # 2×2000 тиков
```

| Критерий | Порог | Факт |
|---|---|---|
| Прирост средней награды к жадному базису | ≥+10% | **+62.2%** |
| Латентность планирования | ≤30 мс | **1.47 мс** |
| Равный бюджет | расход токенов ≤ базиса | **0 против 0** |

Сценарий состоит из двух половин. Первая — выигрыш за цель: его умеет учить и
базис (`GoalIntelligence` хранит одну полезность на цель). Вторая — выигрыш за
режим «энергия × драйв»: таблицу одной полезности на цель этим не описать, а
модель, ключуемую по паре «состояние, действие», — можно. Именно это утверждает
§M2.1, поэтому именно это сценарий обязан содержать. Первая версия харнесса
содержала только первую половину и честно показывала, что обе руки сходятся к
одному числу: планировщик выигрывал скорость обучения, но не потолок.

Критерий «равного бюджета» тоже переписан. Считать отказы в аренде провалом
неверно: отказ — это бюджет, который **сработал**. Базис не получает отказов
только потому, что ничего не просит. Сравнивается фактический расход токенов —
покупаемого ресурса; лишнее процессорное время планирования уже ограничено
бюджетом латентности.

### Этап 5 — политика поведения (§M3.8)

```
python scripts/ab_policy.py                # 3×1500 тиков
```

Три вопроса, и третий держит первые два честными:

1. `behaviour_delta_rate > 0` — политика изменила хотя бы одно решение;
2. средняя награда с политикой не ниже, чем без неё (порог — неухудшение, а не
   процент: задача контура — **убирать** то, что не работает, и на мире, где
   работает всё, он обязан корректно ничего не делать);
3. на чистом шуме — **ноль** активных правил.

Прогон 3×2000 тиков:

| Критерий | Порог | Факт |
|---|---|---|
| `behaviour_delta_rate` | > 0 | **0.373** |
| Награда с политикой против без неё | не ниже | **0.4740 против 0.4170** (+13.7%; на второй половине 0.5129 против 0.3926) |
| Активных правил на чистом шуме | 0 | **0** |
| Активных правил в основном прогоне | — | 6 |

Первая версия сценария была честно красной по пункту 1, и причина оказалась не в
контуре: у живого субстрата `energy` никогда не опускается ниже `hi`
(измерено: 340 «hi» и 60 «mid» из 400 тиков), потому что в офлайновом харнессе
её нечему тратить, а REFLECT восполняет её каждые двадцать тиков. Ловушка,
построенная на `energy == lo`, не срабатывала ни разу, майнер видел одну
константную колонку и правильно ничего не находил.

Поэтому сценарий теперь **навязывает миру режимы** — цикл энергии с периодом,
намеренно много короче блока испытания, чтобы каждый блок ABAB содержал оба
режима и сравнение шло между руками, а не между эпохами.

## A6. Gherkin-сценарии

| Файл | Сценариев | Контур |
|---|---|---|
| `planning.feature` | 8 | план строится, гейты применяются в фиксированном порядке |
| `behaviour_change.feature` | 12 | опыт → правило → изменение поведения → отмена правила |
| `higher_systems.feature` | 10 | пять исходных систем |
| `safety_and_resilience.feature` | 12 | песочница, control plane, эволюция, лог опыта |

## A7. Статистика как отдельный предмет проверки

`tests/test_stats_inference.py` сверяет реализацию с аналитически известными
значениями, а не с тем, что вернул код: t-распределение при df = 1 и df = 2
имеет замкнутую форму, `t.test(1:5, 6:10)` в R даёт t = −5, df = 8, p = 0.001053,
пример Бенджамини–Хохберга 1995 года отвергает ровно две гипотезы из восьми.

Здесь же закрыт дефект, который иначе сделал бы весь контур M3 декоративным:
**Welch-тест при нулевой дисперсии в обеих выборках возвращал p = 1**. Идеально
воспроизводимый эффект — самое сильное свидетельство, какое бывает, — оказывался
«незначимым», и правило с чистым сигналом не активировалось никогда, а с шумным
активировалось. Теперь:

* майнер правил сравнивает **доли** двухвыборочным z-тестом (успехи — это
  пропорции, а не измерения; t-тест по индикаторам 0/1 делит на нулевую
  стандартную ошибку ровно там, где сигнал чист);
* испытание правила сравнивает награды через `compare_samples`: Welch там, где
  он определён, и Mann–Whitney там, где нет. Выбор делается по **форме данных**
  и всегда до того, как посчитан любой результат.

## A8. Процедура аудита

Порядок, которым найдены дефекты A1–A8:

1. **Статический проход.** `ruff --select F,E9,B` по пакету → `F821` в
   `phases/act.py`. Отсюда родился гейт №6.
2. **Прогон гейтов на чистом дереве** — не редактируя исходники во время
   прогона.
3. **Чтение контуров по инварианту, а не по строкам.** Для каждого механизма:
   что он обещает и что случится, если он соврёт. Так найдены A2 и A7.
4. **Прогон приёмочных харнессов.** A7 и A8 нашлись именно здесь: харнесс
   показал 3816 отказов в аренде и падение прироста с +113% до +84%.
5. **Проверка тестов на пустоту.** Каждая регрессия проверяется откатом
   исправления: тест, проходящий и без правки, ничего не защищает.

### Что делать, когда гейт красный

* **Покрытие.** Не понижать порог. Смотреть `--cov-report=term-missing`:
  непокрытыми почти всегда оказываются *деградационные* пути (контур
  недоступен, модель ответила мусором, аренды нет) — то есть ровно те, ради
  которых гейт существует.
* **Мутация.** Выживший мутант — не повод пометить его эквивалентным. Сначала
  выяснить, какого утверждения не хватает.
* **Предохранители.** Красный `test_guarded_files.py` означает изменение одного
  из пяти файлов Приложения H. Это допустимо, но требует отдельного аудита
  безопасности и обновления ожидаемого хеша **в том же коммите**.

---

# Приложение. Этапы 6–7 (прогон от 28.07.2026)

## B1. Этап 6 — пул оценки, сплиты, генераторы

| Гейт | Факт |
|---|---|
| Мутационный балл `eval/generators.py` | **100%** (22/22) |
| Мутационный балл `eval/pool.py` | **100%** (20/20) |
| Мутационный балл `eval/isolated.py` | **100%** (6/6) |
| Мутационный балл `eval/benchmark.py` | **100%** (18/18) |
| Мутационный балл `eval/solver.py` | **100%** (22/22) |
| Покрытие `pool.py`, `generators.py`, `isolated.py` | **100%** |
| Тест утечки held-out | зелёный |
| Тест изоляции оценки | зелёный |

Три решения, которые стоит назвать.

**Проверка на утечку — пер-вид, а не общая.** Бенчмарк переиспользует полезные
нагрузки между видами: `"hello"` — train-случай `reverse` и одновременно
held-out-случай `palindrome`. Общая проверка была бы вечно красной по причине,
которая утечкой не является: написание навыка для `reverse` ничего не говорит об
ответе для `palindrome`.

**Сплит по хешу id, не по позиции.** Добавление задачи не пересдаёт остальные —
иначе каждый записанный ранее valid/test-скор начал бы описывать другой набор.

**Оценка — это данные.** «Применить, померить, вернуть» неверно неочевидным
образом: бенчмарк **записывает**, какие навыки сработали, поэтому измерение
варианта обучает счётчики, по которым живой solver ранжирует. Через десять
вариантов библиотека обучена экспериментом, который никто не собирался
использовать для обучения.

Приёмочный тест генераторов — approval-тест: мутанты, сдвигавшие арифметику
выбора слов, порождали идеально самосогласованные задачи, которые были просто
**другими**, и ничто больше этого не замечало.

## B2. Этап 7 — популяционная эволюция

| Критерий §M5.9 | Порог | Факт |
|---|---|---|
| Монотонность (чемпион не регрессирует) | 0 регрессий | **0** |
| `valid_test_gap` | ≤0.05 | **0.0009** |
| Прирост фитнеса за 20 поколений | ≥+15% | **+3.9% — красный** |
| Мутационный балл модулей эволюции | 0 выживших | см. ниже |

**Почему прирост красный, и почему это не дефект контура.** Фитнес — половина
от навыков, половина от поведенческого роллаута. Половина навыков ограничена
сверху четырьмя видами задач, для которых навыка нет вообще и которые никакой
ген не чинит (~0.7). Половина роллаута на 60 тиках даёт примерно 0.60–0.75.
Достижимый интервал суммарного фитнеса — около 0.65–0.73, то есть **весь
доступный размах меньше 15%**. Поиск при этом работает: за 20 поколений он
находит 2 промоушена, ни разу не регрессирует и держит `valid_test_gap` на
0.0009 при пороге 0.05.

Это ограничение **функции фитнеса**, а не поиска, и его закрывают этапы 8–9:
как только появится бенчмарк рассуждений с генераторами, у половины навыков
появится потолок, до которого действительно можно расти, и гены `reason_*`
перестанут быть инертными. До тех пор критерий честно красный.

**Геном заменён целиком.** Старый содержал `learning_rate`, `dropout`,
`attention_heads` — гиперпараметры LoRA, которых бенчмарк не касается.
Эволюция добросовестно вращала их тысячи тиков в пространстве без градиента, и
все тесты при этом проходили. Теперь каждый ген называет контур, который его
читает, а тест `test_every_delivered_gene_reaches_a_live_contour` ставит ген в
оба конца диапазона и требует, чтобы конфигурация системы изменилась.

**Три дефекта, найденные при сдаче этапа:**

| № | Дефект | Последствие |
|---|---|---|
| B1 | Оценка варианта рекурсивно запускала оценку: планировщик внутри роллаута выбирал `evolve_generation` | неограниченная рекурсия; один роллаут занимал 17.8 с вместо 3.6 с |
| B2 | Геном и `ROITracker` нормировали доли ресурсов **по-разному** | доля, записанная как 0.05, читалась как 0.09 — геном нельзя было вернуть точно, и каждый отвергнутый вариант оставлял след |
| B3 | `evolve_generation` выполнялось внутри тика | поколение — минуты работы против бюджета ACT в 20 мс; тик останавливался целиком |
| B4 | Любой длинный прогон мог запустить поколение | измерено: 137 процессов Python и набор тестов, который перестал завершаться |

B3 закрыт так же, как бенчмарк и обучение весов: действие **запускает**
поколение отсоединённой задачей, а предусловие отказывает, пока задача жива.

B4 закрыт переключателем `AEGIS_EVO_ENABLED` (по умолчанию включён). Он гейтит
**действие**, а не движок: `run_generation` вызывается напрямую всегда, и тесты
про эволюцию от этого не зависят. Набор тестов выключает его автоиспользуемой
фикстурой, а `test_evolution_is_off_in_the_suite` не даёт этому стать
молчаливым допущением.
